import React from "react";
import "./Switcher.css";
export default function Switcher({ checked, onChange, name, textAligh, }: SwitcherI): JSX.Element;
export interface SwitcherI {
    checked?: boolean;
    onChange?: () => void;
    name?: React.ReactNode;
    textAligh?: "left" | "right";
}
