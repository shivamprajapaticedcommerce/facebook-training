import React from "react";
interface BreadcrumbI {
    items: ObjI[];
    onClick?: (value: number, obj?: ObjI) => void;
}
interface ObjI {
    label: string | React.ReactNode;
    value: number;
}
declare function Breadcrumb({ items, onClick, }: BreadcrumbI): JSX.Element;
declare namespace Breadcrumb {
    var defaultProps: {
        items: {
            label: string;
            value: number;
        }[];
    };
}
export default Breadcrumb;
