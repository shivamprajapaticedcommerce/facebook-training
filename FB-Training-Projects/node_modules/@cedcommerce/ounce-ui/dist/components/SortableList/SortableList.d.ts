/// <reference types="react" />
export interface SortableListI {
    items: string[];
    onShuffle?: (items: string[]) => void;
    title?: string;
}
declare function SortableList({ title, items, onShuffle, }: SortableListI): JSX.Element;
export default SortableList;
